const USER_COOKIE = 'user-type';
const USER_ID = 'user-id';

const JWT_TOKEN = 'cookie-jwt';
const PROFILE_STATUS = 'cookie-status';

const UserConsts = {
    USER_COOKIE, USER_ID, JWT_TOKEN, PROFILE_STATUS
};

export default UserConsts;
