import * as React from 'react';
import LoginForm from '../Components/LoginForm';

 const Login = props => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default Login
